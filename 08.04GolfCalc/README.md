# Golf Handicap Calculator

This is straightforward. Sum up all the differentials and then divide by the size to get the average. The round function converts to integers, so do some arithmetic to get the right answer. 