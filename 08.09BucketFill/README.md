# Filling buckets
Straightforward average. Weighted average you actually divide by sum of weights, not by the total amount. That was the only hiccup. 
Since the arrays are the same size, we can step through each one and calculate our averages one at a time.